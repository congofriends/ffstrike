<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put, id: 'edit-event-form' }) do |f| %>

  <%= devise_error_messages! %>

  <div><%= f.label :name, "Name *" %><br />
  <%= f.text_field :name, class: 'form-control edit-form', :autofocus => true, :validate => true %></div><br />

  <div><%= f.label :email, "Email *" %><br />
  <%= f.text_field :email, class: 'form-control edit-form', :autofocus => true, :validate => true %></div><br />

   <div><%= f.label :phone %><br />
  <%= f.text_field :phone, class: 'form-control edit-form', :autofocus => true, :validate => true %></div><br />

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div><%= f.label :password %> <i>(leave blank if you don't want to change it)</i><br />
  <%= f.password_field :password, class: 'form-control edit-form', :autocomplete => "off" %></div>

  <div><%= f.label :password_confirmation %><br />
  <%= f.password_field :password_confirmation, class: 'form-control edit-form' %></div>

  <div><%= f.label :current_password %> <i>(we need your current password to confirm your changes)</i><br />
  <%= f.password_field :current_password, class: 'form-control edit-form' %></div><br />

  <div><%= f.submit "Update", class: 'btn' %></div><br />
<% end %>


<%= button_to "Cancel my account", registration_path(resource_name), class: 'btn btn-danger btn-xs float-right', :data => { :confirm => "Are you sure?" }, :method => :delete %>
