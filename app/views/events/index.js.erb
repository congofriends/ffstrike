function pan() {
      var panPoint = new google.maps.LatLng(<%=@coordinates[0]%>, <%=@coordinates[1]%>);
			map.setZoom(6);
			map.panTo(panPoint);
}
$(".events").html("<%= escape_javascript(render partial: 'events/event', collection: @events, as: :event)%>");

pan();
