.container
  .movement.row
    = image_tag @movement.image(:medium), width: 200, class: 'movement-avatar'
    .movement_info
      %h1
        = @movement.name
      %h2{:class=> "tagline"}= @movement.tagline

      - if current_user && @movement.users.include?(current_user)
        = link_to @movement.name + "'s Dashboard", dashboard_movement_path(@movement, anchor: "movement"), :class=> "btn btn-success btn-sm"
      %h3= @movement.call_to_action

    - if @movement.about_creator
      %h4 About the Movement Creators
      = @movement.about_creator

    =render 'movement_video'

  .support-network.row
    %h3 Our support network is made up of communities supporting justice in the Congo:

    .row
      = render @movement.sub_movements

    - if current_user
      = link_to "Join our support network!", new_submovement_path(), :class=> "btn btn-success btn-sm"
    - else
      = link_to "Join our support network!", new_unauthenticated_submovement_path(), :class => "btn btn-success btn-lg"

  .event.row
    %h3 We are spreading awareness and promoting engagement through these events:

    .row
      - @approved_events.each do |event|
        = render event 

    .row
      .col-md-12
        = render 'create_event_button'

    .row
      %span.search_event_span.col-md-12
        Or search for an event near you:
        = form_tag search_movement_path, method: "get", class: 'form-inline', id: 'zip-form' do
          .form-group
            = search_field_tag 'zip', value = nil, options = {placeholder: "ZIP", class: 'form-control'}
            = hidden_field_tag 'id', value = @movement.id
          .form-group
            = button_tag name="Search", class: 'btn btn-sm btn-info'

  #map-canvas.row
