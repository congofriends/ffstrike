- if current_user
  = link_to "Create an event for Congo Week!", event_type_movement_path(@movement), :class=> "btn btn-success btn-lg"
- else
  = link_to "Create an event for Congo Week!", event_type_movement_path(@movement), :class => "btn btn-success btn-lg"
