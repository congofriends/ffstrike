<nav class="whitespace-for-flash-nav navbar navbar-default navbar-fixed-top" role="navigation">
  <div class='container-fluid'>
    <div class='navbar-header'>
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

      
    </div>

    <div class="navbar-collapse collapse navbar-responsive-collapse" id='collapse-1'>
      <ul class="nav navbar-nav">

        <li> <%= render 'layouts/breadcrumb' %> </li>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% unless current_user.nil? || current_user.movements.empty? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Dashboards <b class="caret"></b></a>
            <ul class="dropdown-menu">
              <% current_user.movements.each do |movement| %>
              <li><%= link_to movement.name.capitalize, dashboard_movement_path(movement, anchor: "movement") %></li>
              <% end %>
            </ul>
          </li>
        <% end %>

        <% if current_user%>
            <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%=user_name_or_email.capitalize%><b class="caret"></b></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Profile", user_movements_path %></li>
              <li class="divider"/>
              <li><%= link_to "Create New Movement", new_movement_path %></li>
            </ul>
        <% end %>

        <% if current_user.nil? %>
          <li><%= link_to "Sign In", new_session_path(:user) %></li>
        <% else %>
          <li><%= link_to 'Sign Out', destroy_user_session_path, :method => :delete %></li>
        <% end %>

      </ul>
    </div>
  </div>
</nav>
