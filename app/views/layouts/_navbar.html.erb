<div class='container'>
  <nav class="navbar navbar-default navbar-static-top" role="navigation">
    <div class='container-fluid'>
      <div class='navbar-header'>
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#collapse-1">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>


      </div>

      <div class="navbar-collapse collapse navbar-responsive-collapse" id='collapse-1'>
        <ul class="nav navbar-nav">

          <li> <%= render 'layouts/breadcrumb' %> </li>
        </ul>

        <ul class="nav navbar-nav navbar-right">

          <% if current_user%>
              <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%=user_name_or_email.capitalize%><b class="caret"></b></a>

              <ul class="dropdown-menu">
              <li><%= link_to "My Profile", user_movements_path %></li>
              <% unless current_user.movements.empty? %>
                <li><%= link_to "Manage My Groups", my_groups_path %></li>
              <% end %>

                
                <li class="divider"/>
                <li><%= link_to "Create Group", new_movement_path %></li>
                <li><%= link_to "Create Event", new_movement_event_path(Movement.first) if Movement.first %></li>
                <li class="divider"/>
                <li><%= link_to 'Sign Out', destroy_user_session_path, :method => :delete %></li>
              </ul>
          <% end %>

          <% if current_user.nil? %>
            <li><%= link_to "Sign in", new_session_path(:user), id: "sign-in" %></li>
          <% end %>

        </ul>
      </div>
    </div>
  </nav>
</div>
