<!DOCTYPE html>
<html lang="en">
<%= render "layouts/head" %>
<%= render "layouts/navbar" %>
  <body>
    <% flash.each do |key, value| %>
      <div class='flash alert alert-success alert-<%= key %>'><%= value %></div>
    <% end %>
    <div class="whitespace-for-flash container">
      <div class="row">
      </div>
      <%= yield %>
    </div> 
  </body>
</html>
