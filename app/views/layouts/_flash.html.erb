<div class="container">
  <% flash.each do |key, value| %>
  <% binding.pry %>
    <div class="row">
      <div class='flash alert alert-success alert-<%= key %>'><%= value %></div>
    </div>
  <% end %>
</div>
